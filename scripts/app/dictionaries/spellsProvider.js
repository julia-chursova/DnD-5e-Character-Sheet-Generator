(function() {
    'use strict';

    angular.module(appName)
        .factory('spellsProvider', [
            function() {
                return {

                };
            }
        ]);
})();