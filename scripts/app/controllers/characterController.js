(function () {
    'use strict';

    angular.module(appName)
        .controller('characterController', [
            'characterModel',
            function (characterModel) {
                var self = this;
            }]);
})();