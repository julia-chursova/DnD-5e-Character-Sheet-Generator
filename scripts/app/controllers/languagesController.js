(function () {
    'use strict';

    angular.module(appName)
        .controller('languagesController', [
            'languagesModel',
            function (languagesModel) {

            }]);
})();